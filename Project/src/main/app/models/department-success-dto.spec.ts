import { DepartmentSuccessDto } from './department-success-dto';

describe('DepartmentSuccessDto', () => {
  it('should create an instance', () => {
    expect(new DepartmentSuccessDto()).toBeTruthy();
  });
});
